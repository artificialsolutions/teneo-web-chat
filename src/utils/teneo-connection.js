// Write a basic one - assuming promise and fetch are available.

function react(engineUrl, session, stream /* true/false */, message) {
    return false;
}
function endSession(engineUrl, session) {
    return false;
}